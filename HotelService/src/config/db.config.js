const path = require("path");

const config = {
  development: {
    username: "root",
    password: "ddas4548",
    database: "airbnb_dev",
    host: "localhost",
    dialect: "mysql",
  },
};

module.exports = config;
